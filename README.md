## 実行方法 (Usage)

本プロジェクトのROS環境を起動するには、用途に合わせて以下のコマンドを実行する。

### 1. ROS1の起動
SIA5の動作計画や軌道生成のシミュレーションを行う場合は、こちらのコマンドを使用する。

```bash
roslaunch motoman_moveit_config demo.launch
```
```bash
roslaunch sia5_moveit sia5_moveit.launch
```
